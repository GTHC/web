version: '3'

volumes:
  db-data:
    external: false

services:
  db:
    image: postgres
    volumes:
      - db-data:/var/lib/postgresql/db-data

  app:
    build: .
    volumes:
      - .:/usr/src/app
    ports:
     - "5000:5000"
     - "3035:3035"
    depends_on:
      - db
